---
interface Props {
  lab: {
    name: string;
    descripcion: string;
    urlDemo: string;
    urlRepo: string;
    imgUrl: string;
  };
}
const { lab } = Astro.props;
---

<div class="container-lab">
  <div class="titulos-lab">
    <h1>{lab.name}</h1>
    <h2>{lab.descripcion}</h2>
    <div class="btn-lab">
      <a class="btn btn-azul-lab" href={lab.urlDemo} target="_blank">Ver demo</a
      >
      <a class="btn btn-gris-lab" href={lab.urlRepo} target="_blank">Repo</a>
    </div>
  </div>
  <div class="box-img">
    <img src={lab.imgUrl} alt="" />
  </div>
</div>

<style>
  .container-lab {
    background-color: var(--color-secundario);
    font-family: var(--fuente-principal);
    padding: 20px;
    display: flex;
    flex-direction: column;
    justify-content: space-between;

    margin: 20px;
    border-radius: 20px;
    background: var(--color-card);
    /*  box-shadow:
      5px 5px 10px #bebebe98,
      -2px -2px 20px #ffffff5a; */
  }
  .container-lab:hover {
    transform: scale(1.01);
  }
  .box-img {
    padding-top: 20px;
    width: 100%;
    object-fit: contain;
  }
  .box-img img {
   
    width: 100%;
    object-fit: cover;
    border-radius: 5px;
  }
  .titulos-lab h1 {
    font-size: 36px;
    padding-bottom: 10px;
    color: var(--color-fuente);
  }
  .titulos-lab h2 {
    font-size: 20px;
    padding-bottom: 15px;
    color: var(--color-fuente);
  }
  .btn-lab {
    display: flex;
    gap: 20px;
  }
  .btn {
    font-weight: 300;
    font-size: 16px;
    color: white;
    padding: 10px 20px;
    border-radius: 5px;
  }

  .btn-azul-lab {
    background-color: var(--color-primario);
  }
  .btn-gris-lab {
    background-color: var(--color-terciario);
  }

  @media (min-width: 700px) {
    .container-lab {
      width: 1165px;
      height: 416px;
      display: flex;
      flex-direction: row;
    }

    .box-img {
      width: 50%;
    }

    .box-img img {
      height: 376px;
    }
    .titulos-lab {
      padding-bottom: 180px;
      padding-left: 30px;
    }
  }
</style>
